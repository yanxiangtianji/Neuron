import math
import process_base as base
import myalogirthm as alg

def neuron_correlation(line_f,line_t,i_rng_f,i_rng_t,t_rng,T):
	res=[]
	l=len(line_t)
	st=t_rng[0]+T
	end=t_rng[1]+1e-8 if typeof(st)==float else t_rng[1]
	while st<=end
		for v in line_f:
			pass
			
	return res
	
def neuron_pairs_correlation(data,t_rng,T):
	n_node=len(data)
	res=[[[] for i in range(n_node)] for i in range(n_node)]
	for i in range(n_node):
		for j in range(n_node):
			pass
